---
tags:
  - http
---
---
previously, we understood about `HTTP Servers`:

### Now we’re learning

1. [[Headers]]
2. [[Fetch API]] in the browser
3. [[HTTP Servers]]
4. Query params, Creating our own HTTP Server (Again). Does simple math (sum server)
5. Middlewares
    1. Create a middleware function that logs each incoming request’s HTTP method, URL, and timestamp to the console
    2. Count total number of requests
6. Commonly used middlewares
    1. [express](https://expressjs.com/).json / [bodyParser](https://www.npmjs.com/package/body-parser).json
    2. [CORS](https://www.npmjs.com/package/cors)
