remove deny and allow list examples, 
remove massive repitions,
ensure imports are at top of file,

create 3 hosts
remove CIDR and do it simply 

The entire example is to have 2 hosts and use the nodes to connect with each other 

---

Fundamentally 3 hosts, that have allow and deny list. create hosts connection and 